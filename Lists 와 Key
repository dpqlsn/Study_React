### 1. List
자바스크립트의 변수나 객체들을 하나의 변수로 묶어 놓은 것으로 배열을 보여줌

### 2. Key
각 객체나 아이템을 구분할 수 있는 고유한 값 즉, 아이템들을 구분하기 위한 고유한 문자열을 의미
ex ) 주민등록번호, 학번 등등..

**Key의 값은 같은 List에 있는 Elements 사이에서만 고유한 값이면 된다**

const person = {
    <!-- 이때 name는 key, 'Yebeen'은 value -->
	name : 'Yebeen';
}

### 3. map()
여러개의 컴포넌트를 렌더링하기 위함
배열에 들어있는 각 변수에 어떤 처리를 한 뒤 리턴하는 것

**map()함수 안에 있는 Elements는 key가 꼭 찰요함**